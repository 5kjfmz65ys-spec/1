PORT=3080
APP_URL=http://localhost:3080
DATA_DIR=./data
# 32-byte key encoded as 64 hex chars or base64. In development the app generates a local key file if omitted.
ENCRYPTION_KEY=
SESSION_TTL_DAYS=30
COOKIE_SECURE=false
META_GRAPH_API_VERSION=
WA_GATEWAY_URL=http://localhost:3090
WA_GATEWAY_TOKEN=replace-with-a-long-random-token
INTERNAL_WEBHOOK_TOKEN=replace-with-a-long-random-token
