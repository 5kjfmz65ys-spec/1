# تشغيل Company OS من الجوال

هذه الصفحة تجهّز المشروع تلقائيًا داخل GitHub Codespaces.

## التشغيل

1. افتح فرع `company-os`.
2. اضغط **Code**.
3. افتح تبويب **Codespaces**.
4. اضغط **Create codespace on company-os**.
5. انتظر حتى يكتمل تثبيت الحزم وإعداد قاعدة البيانات.
6. ستفتح معاينة المنفذ `3000` تلقائيًا.

## بيانات الدخول التجريبية

راجع ملف `company-os-app/README.md` بعد اكتمال التجهيز. إذا لم تفتح المعاينة تلقائيًا، افتح تبويب **Ports** واضغط رابط المنفذ `3000`.

## ملاحظات

- يتم استخراج المشروع تلقائيًا من الأرشيف الموجود في هذا الفرع.
- يتم إنشاء ملف `.env` محليًا من `.env.example`، ولا يتم نشر الأسرار.
- سجل التشغيل موجود في `company-os-dev.log`.
