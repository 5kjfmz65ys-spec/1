# تسليم مشروع ردّاد AI

## المنجز

- تطبيق عربي RTL يعمل على Node.js 22 بدون تثبيت حزم للتطبيق الأساسي.
- تسجيل حساب، دخول، جلسات HttpOnly، منشآت وعضويات وأدوار.
- قاعدة SQLite محلية مع عزل بيانات المنشآت.
- تشفير AES-256-GCM لمفاتيح Meta وAI.
- صفحة ربط رسمي وحفظ بيانات Meta واختبار الاتصال وإرسال النصوص.
- Meta Webhook مع التحقق من التوقيع ومنع التكرار وتطبيع الرسائل والحالات.
- ربط تجريبي اختياري مع تحذيرين وموافقة موثقة قبل إنشاء الجلسة.
- بوابة Baileys مستقلة تدعم Pairing Code والحالة واستقبال الرسائل وإرسال النص.
- Inbox بثلاثة أعمدة، رد يدوي، إيقاف وتشغيل المساعد وإغلاق المحادثة.
- قاعدة معرفة نصية وبحث محلي.
- مساعد مقيد بنطاق المتجر مع مخرجات منظمة وتحويل لموظف.
- BYOK: Ollama، OpenAI-compatible، Anthropic، Gemini.
- منتجات وطلبات وفريق وتقارير وإعدادات منشأة.
- بيانات تجربة واختبارات وتوثيق وDocker.

## نتائج الفحص الفعلية

- `npm run verify`: ناجح.
- `npm test`: ناجح، 6 اختبارات من 6.
- `node --check`: ناجح للملفات الرئيسية والخادم والواجهة والبوابة.
- تشغيل الخادم والاتصال بـ`/api/health`: ناجح.
- تسجيل الدخول بالحساب التجريبي: ناجح.
- تحميل `/api/me` و`/api/dashboard`: ناجح.
- اختبار المساعد من قاعدة المعرفة: ناجح.

## لم يُختبر فعليًا

- اتصال Meta حي وإرسال رسالة حقيقية، لعدم وجود بيانات حساب Meta.
- جلسة Baileys حية، لأن الحزمة الاختيارية غير متوفرة في بيئة البناء ولم يتم ربط رقم حقيقي.
- Anthropic وGemini وOpenAI وOllama الحية، لعدم وجود مفاتيح وخدمات.
- Docker build، لأن Docker غير متاح في بيئة البناء.

## تشغيل المشروع

```bash
cp .env.example .env
npm run seed
npm start
```

ثم افتح `http://localhost:3080`.

حساب التجربة:

- `demo@reddad.local`
- `Demo12345!`

## الخطوة المطلوبة للإطلاق التجاري

انقل SQLite إلى PostgreSQL، استخدم Redis للطوابير ومحدد المعدل، أضف تخزين وسائط آمن، اختبر Meta في بيئة Staging، واستبدل تخزين جلسات Baileys المحلي بمخزن مشفر.
